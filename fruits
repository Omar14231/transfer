-- Buddha Tool - Giant Yellow Mode
-- By: [Your Name]

local Players = game:GetService("Players")
local PlayerGui = Players.LocalPlayer:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")

-- إنشاء الأداة
local tool = Instance.new("Tool")
tool.Name = "Buddha"
tool.ToolTip = "Buddha - Giant Yellow Mode"
tool.TextureId = "rbxassetid://11337649779"
tool.CanBeDropped = false
tool.RequiresHandle = false

-- متغيرات النظام
local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local isBigSize = false
local originalColors = {}
local originalWalkSpeed = 16

-- 🎯 تفعيل الوضع العملاق الأصفر
local function activateGiantYellowMode()
    if isBigSize then return end
    
    isBigSize = true
    print("🟡 تفعيل الوضع العملاق الأصفر")
    
    -- حفظ الألوان الأصلية
    for _, part in pairs(character:GetChildren()) do
        if part:IsA("BasePart") then
            originalColors[part] = part.BrickColor
        end
    end
    
    -- حفظ سرعة المشي الأصلية
    originalWalkSpeed = humanoid.WalkSpeed
    
    -- ⭐ التكبير إلى حجم عملاق
    character:SetScale(Vector3.new(5, 5, 5)) -- 5 أضعاف الحجم الطبيعي
    
    -- ⭐ جعل اللون أصفر لامع
    for _, part in pairs(character:GetChildren()) do
        if part:IsA("BasePart") then
            part.BrickColor = BrickColor.new("Bright yellow")
            part.Material = Enum.Material.Neon -- جعلها مضيئة
        end
    end
    
    -- ⭐ زيادة سرعة المشي
    humanoid.WalkSpeed = 25
    
    -- ⭐ تأثيرات بصرية
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if rootPart then
        -- تأثير صوتي للتكبير
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://9111278363"
        sound.Volume = 1.0
        sound.Parent = rootPart
        sound:Play()
        game:GetService("Debris"):AddItem(sound, 3)
        
        -- تأثير اهتزاز
        spawn(function()
            for i = 1, 15 do
                rootPart.CFrame = rootPart.CFrame * CFrame.new(0, 0.2, 0)
                wait(0.03)
                rootPart.CFrame = rootPart.CFrame * CFrame.new(0, -0.2, 0)
                wait(0.03)
            end
        end)
        
        -- إضافة توهج أصفر قوي
        local glow = Instance.new("PointLight")
        glow.Name = "BuddhaGlow"
        glow.Brightness = 15
        glow.Color = Color3.fromRGB(255, 255, 0) -- أصفر ساطع
        glow.Range = 30
        glow.Shadows = true
        glow.Parent = rootPart
    end
    
    -- ⭐ جعل جميع الأجزاء مضيئة
    for _, part in pairs(character:GetChildren()) do
        if part:IsA("BasePart") then
            local partGlow = Instance.new("PointLight")
            partGlow.Name = "PartGlow"
            partGlow.Brightness = 8
            partGlow.Color = Color3.fromRGB(255, 255, 100) -- أصفر فاتح
            partGlow.Range = 15
            partGlow.Parent = part
        end
    end
end

-- 🎯 العودة للحالة الطبيعية
local function deactivateGiantMode()
    if not isBigSize then return end
    
    isBigSize = false
    print("🔵 العودة للحالة الطبيعية")
    
    -- ⭐ إرجاع الحجم الطبيعي
    character:SetScale(Vector3.new(1, 1, 1))
    
    -- ⭐ إرجاع الألوان الأصلية
    for part, originalColor in pairs(originalColors) do
        if part and part.Parent then
            part.BrickColor = originalColor
            part.Material = Enum.Material.Plastic -- إرجاع المادة الأصلية
        end
    end
    
    -- ⭐ إرجاع سرعة المشي
    humanoid.WalkSpeed = originalWalkSpeed
    
    -- ⭐ إزالة التوهج
    for _, part in pairs(character:GetChildren()) do
        if part:IsA("BasePart") then
            local glow = part:FindFirstChild("BuddhaGlow")
            if glow then
                glow:Destroy()
            end
            local partGlow = part:FindFirstChild("PartGlow")
            if partGlow then
                partGlow:Destroy()
            end
        end
    end
    
    -- تنظيف
    originalColors = {}
end

-- 🎯 تبديل الوضع
local function toggleGiantMode()
    if isBigSize then
        deactivateGiantMode()
    else
        activateGiantYellowMode()
    end
end

-- 🎮 أحداث الأداة
tool.Activated:Connect(function()
    toggleGiantMode()
end)

tool.Equipped:Connect(function()
    print("🟡 Buddha tool equipped - Click to become giant yellow")
end)

tool.Unequipped:Connect(function()
    -- العودة تلقائياً عند ترك الأداة
    if isBigSize then
        deactivateGiantMode()
    end
end)

-- 🔄 تحديث الشخصية عند الموت
player.CharacterAdded:Connect(function(newCharacter)
    character = newCharacter
    humanoid = character:WaitForChild("Humanoid")
    
    -- إعادة تعيين المتغيرات
    isBigSize = false
    originalColors = {}
end)

-- 🚀 إضافة الأداة للاعب
tool.Parent = Players.LocalPlayer.Backpack

-- 🔧 زر طوارئ للعودة
local UserInputService = game:GetService("UserInputService")
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    -- زر R للعودة الطارئة
    if input.KeyCode == Enum.KeyCode.R and isBigSize then
        deactivateGiantMode()
    end
end)

print("🟡 Buddha Giant Yellow tool loaded successfully!")
print("🎮 Click the tool to become giant yellow")
print("🔴 Press R to emergency return to normal")
