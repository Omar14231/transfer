-- Buddha Tool - Size & Light Power
-- By: [Your Name]

local Players = game:GetService("Players")
local PlayerGui = Players.LocalPlayer:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

-- إنشاء الأداة
local tool = Instance.new("Tool")
tool.Name = "Buddha"
tool.ToolTip = "Buddha - Size & Light Power"
tool.TextureId = "rbxassetid://11337649779"
tool.CanBeDropped = false
tool.RequiresHandle = false

-- متغيرات النظام
local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local isBigSize = false
local originalSize = Vector3.new(1, 1, 1)
local originalWalkSpeed = 16
local lights = {}

-- 🎯 تفعيل وضع Buddha الكبير
local function activateBuddhaMode()
    if isBigSize then return end
    
    isBigSize = true
    print("🟡 تفعيل وضع Buddha الكبير")
    
    -- حفظ الأحجام الأصلية
    originalSize = character:GetScale()
    originalWalkSpeed = humanoid.WalkSpeed
    
    -- تكبير الشخصية
    character:SetScale(Vector3.new(3, 3, 3)) -- 3 أضعاف الحجم الطبيعي
    
    -- زيادة سرعة المشي
    humanoid.WalkSpeed = 30
    
    -- إضافة أضواء صفراء لجميع الأطراف
    for _, part in pairs(character:GetChildren()) do
        if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
            local light = Instance.new("PointLight")
            light.Name = "BuddhaLight"
            light.Brightness = 5
            light.Color = Color3.fromRGB(255, 255, 0) -- أصفر
            light.Range = 15
            light.Shadows = true
            light.Parent = part
            table.insert(lights, light)
        end
    end
    
    -- إضافة ضوء قوي للرأس
    local head = character:FindFirstChild("Head")
    if head then
        local headLight = Instance.new("PointLight")
        headLight.Name = "BuddhaHeadLight"
        headLight.Brightness = 10
        headLight.Color = Color3.fromRGB(255, 255, 100) -- أصفر فاتح
        headLight.Range = 25
        headLight.Shadows = true
        headLight.Parent = head
        table.insert(lights, headLight)
    end
    
    -- تأثير اهتزاز الأرض
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if rootPart then
        -- تأثير صوتي
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://9111278363"
        sound.Volume = 1.0
        sound.Parent = rootPart
        sound:Play()
        game:GetService("Debris"):AddItem(sound, 3)
        
        -- تأثير اهتزاز
        spawn(function()
            for i = 1, 10 do
                rootPart.CFrame = rootPart.CFrame * CFrame.new(0, 0.1, 0)
                wait(0.05)
                rootPart.CFrame = rootPart.CFrame * CFrame.new(0, -0.1, 0)
                wait(0.05)
            end
        end)
    end
end

-- 🎯 تعطيل وضع Buddha والعودة للحجم الطبيعي
local function deactivateBuddhaMode()
    if not isBigSize then return end
    
    isBigSize = false
    print("🔵 تعطيل وضع Buddha - العودة للحجم الطبيعي")
    
    -- إرجاع الحجم الطبيعي
    character:SetScale(originalSize)
    
    -- إرجاع سرعة المشي الطبيعية
    humanoid.WalkSpeed = originalWalkSpeed
    
    -- إزالة جميع الأضواء
    for _, light in pairs(lights) do
        if light and light.Parent then
            light:Destroy()
        end
    end
    lights = {}
    
    -- تنظيف أي أضواء متبقية
    for _, part in pairs(character:GetChildren()) do
        if part:IsA("BasePart") then
            local remainingLight = part:FindFirstChild("BuddhaLight")
            if remainingLight then
                remainingLight:Destroy()
            end
            local headLight = part:FindFirstChild("BuddhaHeadLight")
            if headLight then
                headLight:Destroy()
            end
        end
    end
end

-- 🎯 نظام الضرب عند النقر على الشاشة
local function setupScreenPunch()
    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then return end
        
        -- النقر على الشاشة (ليس الأداة)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or 
           input.UserInputType == Enum.UserInputType.Touch then
            
            if isBigSize then
                -- تأثير الضرب
                local rootPart = character:FindFirstChild("HumanoidRootPart")
                if rootPart then
                    -- إنشاء تأثير موجة صدمة
                    local shockwave = Instance.new("Part")
                    shockwave.Name = "BuddhaPunch"
                    shockwave.Size = Vector3.new(10, 10, 10)
                    shockwave.Position = rootPart.Position + rootPart.CFrame.LookVector * 10
                    shockwave.Anchored = true
                    shockwave.CanCollide = false
                    shockwave.Material = Enum.Material.Neon
                    shockwave.BrickColor = BrickColor.new("Bright yellow")
                    shockwave.Transparency = 0.3
                    shockwave.Parent = workspace
                    
                    -- تأثير توهج
                    local shockwaveLight = Instance.new("PointLight")
                    shockwaveLight.Brightness = 20
                    shockwaveLight.Color = Color3.fromRGB(255, 255, 0)
                    shockwaveLight.Range = 15
                    shockwaveLight.Parent = shockwave
                    
                    -- صوت الضرب
                    local punchSound = Instance.new("Sound")
                    punchSound.SoundId = "rbxassetid://2785498977"
                    punchSound.Volume = 0.8
                    punchSound.Parent = shockwave
                    punchSound:Play()
                    
                    -- تأثير الاختفاء
                    spawn(function()
                        for i = 1, 10 do
                            shockwave.Size = shockwave.Size + Vector3.new(2, 2, 2)
                            shockwave.Transparency = shockwave.Transparency + 0.07
                            wait(0.05)
                        end
                        shockwave:Destroy()
                    end)
                    
                    print("👊 Buddha ضربة قوية!")
                end
            end
        end
    end)
end

-- 🎯 تبديل وضع Buddha
local function toggleBuddhaMode()
    if isBigSize then
        deactivateBuddhaMode()
    else
        activateBuddhaMode()
    end
end

-- 🎮 أحداث الأداة
tool.Activated:Connect(function()
    toggleBuddhaMode()
end)

tool.Equipped:Connect(function()
    print("🟡 Buddha tool equipped - Click to toggle size")
end)

tool.Unequipped:Connect(function()
    -- تعطيل الوضع تلقائياً عند ترك الأداة
    if isBigSize then
        deactivateBuddhaMode()
    end
end)

-- 🔄 تحديث الشخصية عند الموت
player.CharacterAdded:Connect(function(newCharacter)
    character = newCharacter
    humanoid = character:WaitForChild("Humanoid")
    
    -- إعادة تعيين المتغيرات
    isBigSize = false
    lights = {}
end)

-- 🎯 إعداد نظام الضرب
setupScreenPunch()

-- 🚀 إضافة الأداة للاعب
tool.Parent = Players.LocalPlayer.Backpack

-- 🔧 أوامر إضافية للتحكم
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    -- زر R للعودة للحجم الطبيعي (طوارئ)
    if input.KeyCode == Enum.KeyCode.R and isBigSize then
        deactivateBuddhaMode()
    end
end)

print("🟡 Buddha Size & Light tool loaded successfully!")
print("🎮 Click the tool to toggle giant size")
print("👊 Click screen to punch while in Buddha mode")
print("🔴 Press R to emergency return to normal size")
